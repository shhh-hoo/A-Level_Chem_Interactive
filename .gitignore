.DS_Store
node_modules/
dist/
dist-ssr/

# Env files (do NOT commit secrets)
.env
.env.*
*.env

# Allow committing env templates/examples
!.env.example
!.env.sample
!.env.template
!.env.defaults

# Supabase local-only secrets generated by test scripts
supabase/.env.edge

# Project-specific sensitive/demo data
supabase/seed/demo-codes.txt

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage / reports
coverage/

# Lint / tooling caches
.eslintcache
.stylelintcache

# Vite cache (default is node_modules/.vite, but this is safe if cacheDir is changed)
.vite/

# Local-only files (Vite recommends ignoring *.local, including .env.*.local)
*.local

# IDE (optional: keep if you don’t want editor settings in git)
.idea/

# Supabase local CLI artifacts (optional: only if these folders appear)
supabase/.branches/
supabase/.temp/
